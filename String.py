# String Methos

# phone_no = input("Enter your phone number: ")

# length = len(name)

# result = name.find(" ") // find and tell the first occurence else -1
# result = name.rfind("o") // it shows the last occurence of the letter else -1

# result = name.capitalize() # first letter will in capital

# result = name.upper()
# result = name.lower()
# result = name.isdigit() // return true or false if it contain the digits
# result = name.isalpha() // return only alphabatical character else false if it contain digit or space etc

# result = phone_no.count("-") // return the count for which you provide in bracket
# result = phone_no.replace("-"," ") // replace the character with gevin character

print(help(str))